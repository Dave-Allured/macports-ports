--- configure.orig	2025-07-31 15:25:30
+++ configure	2025-12-02 15:37:37
@@ -39792,9 +39792,10 @@
 EOF
     ac_cv_ctype_fortran=no
     { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking if Fortran \"$NF_INT1_T\" is " >&5
-printf %s "checking if Fortran \"$NF_INT1_T\" is ... " >&6; }
+printf "%s\n" "checking if Fortran \"$NF_INT1_T\" is ... " >&6; }
     for ctype in "signed char" short int long; do
-		cat >conftest.c <<EOF
+		printf "%s\n" "   check ctype = $ctype " >&6
+                cat >conftest.c <<EOF
 	    int $FCALLSCSUB($ctype values[4])
 	    {
 		return(values[1] != -2 || values[2] != -3);
@@ -39802,29 +39803,38 @@
 EOF
 	doit='$CC -c ${CPPFLAGS} ${CFLAGS} conftest.c'
 	if { { eval echo "\"\$as_me\":${as_lineno-$LINENO}: \"$doit\""; } >&5
+  printf "%s\n" "     eval compile -c conftest.c" >&6
   (eval $doit) 2>&5
   ac_status=$?
+  printf "%s\n" "       ac_status = $ac_status " >&6
   printf "%s\n" "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
 	    doit='$FC ${FFLAGS} -c conftestf.f'
 	    if { { eval echo "\"\$as_me\":${as_lineno-$LINENO}: \"$doit\""; } >&5
+  printf "%s\n" "     eval compile -c conftestf.f" >&6
   (eval $doit) 2>&5
   ac_status=$?
+  printf "%s\n" "       ac_status = $ac_status " >&6
   printf "%s\n" "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
 	        doit='$FC -o conftest ${FFLAGS} ${FLDFLAGS} conftestf.o conftest.o ${LDFLAGS} ${LIBS}'
 	        if { { eval echo "\"\$as_me\":${as_lineno-$LINENO}: \"$doit\""; } >&5
+  printf "%s\n" "     eval link ./conftest using \$FC " >&6
   (eval $doit) 2>&5
   ac_status=$?
+  printf "%s\n" "       ac_status = $ac_status " >&6
   printf "%s\n" "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
 		    doit=./conftest
 		    if { { eval echo "\"\$as_me\":${as_lineno-$LINENO}: \"$doit\""; } >&5
+  printf "%s\n" "     eval ./conftest" >&6
   (eval $doit) 2>&5
   ac_status=$?
+  (eval $doit) 2>&6
+  printf "%s\n" "       ac_status = $ac_status " >&6
   printf "%s\n" "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-		        		        { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: \"$ctype\" in C" >&5
+		        		        { printf "%s" "$as_me:${as_lineno-$LINENO}: result: \"$ctype\" in C" >&5
 printf "%s\n" "\"$ctype\" in C" >&6; }
 		        cname=`echo $ctype | tr ' abcdefghijklmnopqrstuvwxyz' \
 			    _ABCDEFGHIJKLMNOPQRSTUVWXYZ`
